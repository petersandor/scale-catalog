image:
  pullPolicy: IfNotPresent
  repository: factoriotools/factorio
  tag: stable@sha256:a4f1217887333814fc9b79c544999c80718603bad228c3068009ee9f41c9aae3

securityContext:
  container:
    readOnlyRootFilesystem: false
    runAsNonRoot: false
    runAsGroup: 0
    runAsUser: 0

service:
  main:
    ports:
      main:
        port: 34197
        protocol: udp
  rcon:
    enabled: true
    ports:
      rcon:
        enabled: true
        port: 27015
        protocol: tcp

workload:
  main:
    podSpec:
      containers:
        main:
          probes:
            liveness:
              type: tcp
              port: "{{ .Values.service.rcon.ports.rcon.port }}"
            readiness:
              type: tcp
              port: "{{ .Values.service.rcon.ports.rcon.port }}"
            startup:
              type: tcp
              port: "{{ .Values.service.rcon.ports.rcon.port }}"
          env:
            PORT: "{{ .Values.service.main.ports.main.port }}"
            RCON_PORT: "{{ .Values.service.rcon.ports.rcon.port }}"
            GENERATE_NEW_SAVE: false
            LOAD_LATEST_SAVE: true
            SAVE_NAME: "_autosave1"
            TOKEN: ""
            UPDATE_MODS_ON_START: ""
            USERNAME: ""

persistence:
  config:
    enabled: true
    mountPath: /factorio

portal:
  open:
    enabled: false
